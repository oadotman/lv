# =====================================================
# PADDLE CONFIGURATION
# Copy this to .env.local and fill with your actual values
# =====================================================

# Your Paddle Vendor/Seller ID (visible in Paddle dashboard)
NEXT_PUBLIC_PADDLE_VENDOR_ID=258271

# =====================================================
# MONTHLY PLAN PRICE IDs
# Get these from Paddle Dashboard > Catalog > Prices
# =====================================================

# Solo Plan - $29/month
NEXT_PUBLIC_PADDLE_PRICE_ID_SOLO_MONTHLY=pri_REPLACE_WITH_ACTUAL

# Starter Plan - $49/month
NEXT_PUBLIC_PADDLE_PRICE_ID_STARTER_MONTHLY=pri_REPLACE_WITH_ACTUAL

# Professional Plan - $99/month
NEXT_PUBLIC_PADDLE_PRICE_ID_PROFESSIONAL_MONTHLY=pri_REPLACE_WITH_ACTUAL

# Enterprise Plan - $299/month
NEXT_PUBLIC_PADDLE_PRICE_ID_ENTERPRISE_MONTHLY=pri_REPLACE_WITH_ACTUAL

# =====================================================
# ANNUAL PLAN PRICE IDs (17% discount)
# =====================================================

# Solo Annual - $289/year
NEXT_PUBLIC_PADDLE_PRICE_ID_SOLO_ANNUAL=pri_REPLACE_WITH_ACTUAL

# Starter Annual - $489/year
NEXT_PUBLIC_PADDLE_PRICE_ID_STARTER_ANNUAL=pri_REPLACE_WITH_ACTUAL

# Professional Annual - $989/year
NEXT_PUBLIC_PADDLE_PRICE_ID_PROFESSIONAL_ANNUAL=pri_REPLACE_WITH_ACTUAL

# Enterprise Annual - $2989/year
NEXT_PUBLIC_PADDLE_PRICE_ID_ENTERPRISE_ANNUAL=pri_REPLACE_WITH_ACTUAL

# =====================================================
# OVERAGE PACK PRICE IDs
# =====================================================

# 500 minutes - $10
NEXT_PUBLIC_PADDLE_PRICE_ID_OVERAGE_500=pri_REPLACE_WITH_ACTUAL

# 1000 minutes - $20
NEXT_PUBLIC_PADDLE_PRICE_ID_OVERAGE_1000=pri_REPLACE_WITH_ACTUAL

# 2500 minutes - $50
NEXT_PUBLIC_PADDLE_PRICE_ID_OVERAGE_2500=pri_REPLACE_WITH_ACTUAL

# 5000 minutes - $100
NEXT_PUBLIC_PADDLE_PRICE_ID_OVERAGE_5000=pri_REPLACE_WITH_ACTUAL

# =====================================================
# SERVER-SIDE KEYS (Keep these secret!)
# =====================================================

# Get from Paddle Dashboard > Developer > Authentication
PADDLE_API_KEY=REPLACE_WITH_YOUR_API_KEY

# Get from Paddle Dashboard > Developer > Notifications > Webhooks
PADDLE_WEBHOOK_SECRET=REPLACE_WITH_YOUR_WEBHOOK_SECRET

# =====================================================
# HOW TO GET THESE VALUES:
# =====================================================
#
# 1. Log into your Paddle account at https://vendors.paddle.com
#
# 2. For Price IDs:
#    - Go to Catalog → Prices
#    - Create a price for each plan if not already done
#    - Click on each price to see its ID (starts with pri_)
#    - Copy the ID and paste it above
#
# 3. For API Key:
#    - Go to Developer → Authentication
#    - Create a new API key or use existing one
#    - Copy and paste above
#
# 4. For Webhook Secret:
#    - Go to Developer → Notifications
#    - Set up a webhook endpoint: https://yourdomain.com/api/paddle/webhook
#    - Copy the signing secret
#
# 5. IMPORTANT: After filling this out:
#    - Save as .env.local
#    - Run: npm run build
#    - Restart your application
#    - Upload to your production server
# =====================================================