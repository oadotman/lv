# LoadVoice Production Environment Template
# Copy this file to .env.production on your VPS and fill in the values

# ============================================
# CORE CONFIGURATION (REQUIRED)
# ============================================
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://yourdomain.com  # Replace with your actual domain
NEXT_PUBLIC_APP_VERSION=1.0.0

# ============================================
# SUPABASE (REQUIRED - Copy from .env.local)
# ============================================
NEXT_PUBLIC_SUPABASE_URL=https://wiacusdxiyxjybckappw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=# Copy from .env.local
SUPABASE_SERVICE_ROLE_KEY=# Copy from .env.local

# ============================================
# AI SERVICES (REQUIRED - Copy from .env.local)
# ============================================
ASSEMBLYAI_API_KEY=# Copy from .env.local
OPENAI_API_KEY=# Copy from .env.local

# ============================================
# EXTERNAL SERVICES (Configure after deployment)
# ============================================

# Twilio - Voice & SMS (Apply at twilio.com after site is live)
TWILIO_ACCOUNT_SID=temp_not_configured
TWILIO_AUTH_TOKEN=temp_not_configured
TWILIO_PHONE_NUMBER=+1234567890
TWILIO_API_KEY=temp_not_configured
TWILIO_API_SECRET=temp_not_configured
TWILIO_TWIML_APP_SID=temp_not_configured

# Paddle - Payment Processing (Apply at paddle.com after site is live)
PADDLE_API_KEY=temp_not_configured
PADDLE_CLIENT_TOKEN=temp_not_configured
PADDLE_WEBHOOK_SECRET=temp_not_configured
NEXT_PUBLIC_PADDLE_VENDOR_ID=temp_not_configured
NEXT_PUBLIC_PADDLE_PRICE_ID_BASIC=temp_not_configured
NEXT_PUBLIC_PADDLE_PRICE_ID_PROFESSIONAL=temp_not_configured
NEXT_PUBLIC_PADDLE_PRICE_ID_ENTERPRISE=temp_not_configured
NEXT_PUBLIC_PADDLE_PRICE_ID_BASIC_ANNUAL=temp_not_configured
NEXT_PUBLIC_PADDLE_PRICE_ID_PROFESSIONAL_ANNUAL=temp_not_configured
NEXT_PUBLIC_PADDLE_PRICE_ID_ENTERPRISE_ANNUAL=temp_not_configured
NEXT_PUBLIC_PADDLE_OVERAGE_PRICE_ID=temp_not_configured
NEXT_PUBLIC_PADDLE_ENVIRONMENT=sandbox  # Change to 'production' when ready

# Resend - Email Service (Sign up at resend.com)
RESEND_API_KEY=re_temp_not_configured
RESEND_FROM_EMAIL=hello@yourdomain.com
RESEND_REPLY_TO=support@yourdomain.com

# Redis - Caching & Queue (Optional but recommended)
REDIS_URL=  # redis://localhost:6379 or your Redis URL

# Inngest - Background Jobs (Optional)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# Sentry - Error Monitoring (Optional but recommended)
SENTRY_DSN=
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# ============================================
# SECURITY (GENERATE NEW VALUES!)
# ============================================

# Generate with: openssl rand -hex 32
PARTNER_JWT_SECRET=GENERATE_NEW_SECURE_SECRET_HERE

# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=GENERATE_NEW_SECRET_HERE

# ============================================
# PARTNER PROGRAM (Optional)
# ============================================
NEXT_PUBLIC_PARTNER_PORTAL_URL=https://yourdomain.com/partners
PARTNER_COMMISSION_RATE=0.20  # 20% commission

# ============================================
# MONITORING (Optional)
# ============================================
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=
NEXT_PUBLIC_CLARITY_PROJECT_ID=
NEXT_PUBLIC_HOTJAR_ID=

# ============================================
# NOTES
# ============================================
# 1. MUST CONFIGURE before deployment:
#    - NEXT_PUBLIC_APP_URL (your domain)
#    - Supabase credentials (copy from .env.local)
#    - AI credentials (copy from .env.local)
#    - PARTNER_JWT_SECRET (generate new!)
#    - NEXTAUTH_SECRET (generate new!)
#
# 2. CAN CONFIGURE LATER (after site is live):
#    - Twilio (after verification)
#    - Paddle (after approval)
#    - Resend (whenever ready)
#    - Redis (for better performance)
#    - Sentry (for error tracking)
#
# 3. The app will work without the "CAN CONFIGURE LATER" services
#    but with limited functionality. Feature flags will disable
#    unavailable services gracefully.